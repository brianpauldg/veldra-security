# ============================================================
# Nova Health — Environment Variables
# Copy this to .env.local for development
# Set these in Vercel dashboard for production
# NEVER commit .env.local to git
# ============================================================

# ── SUPABASE ─────────────────────────────────────────────────
# Get from: supabase.com → your project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — SERVER ONLY, never expose to client
# Get from: Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── ENCRYPTION ───────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Must be exactly 64 hex characters (32 bytes = 256 bits)
ENCRYPTION_KEY=generate-a-64-char-hex-string-here

# ── APP URLs ─────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://app.novahealth.io

# For development:
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── STRIPE ───────────────────────────────────────────────────
# Get from: dashboard.stripe.com → Developers → API Keys
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-publishable-key
# Price ID for your subscription product (server only)
STRIPE_PRICE_ID=price_XXXXXXXXXXXXXX
# Optional: trial days for new signups (number)
STRIPE_TRIAL_DAYS=14

# ── ANTHROPIC (for AI checklists) ────────────────────────────
# Get from: console.anthropic.com → API Keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── REGOLOGY (for regulatory data) ───────────────────────────
REGOLOGY_API_KEY=your-regology-key-here

# ── SENDGRID (for emails) ────────────────────────────────────
SENDGRID_API_KEY=SG.your-key-here
SENDGRID_FROM_EMAIL=notifications@novahealth.io

# ── SECURITY ─────────────────────────────────────────────────
# Secret for CSRF tokens — generate with crypto.randomBytes(32).toString('hex')
CSRF_SECRET=generate-another-random-64-char-hex-string

# ── REDIS (for rate limiting in production) ───────────────────
# Optional but recommended — use Upstash for serverless
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# ── VERCEL ───────────────────────────────────────────────────
# Set automatically by Vercel — do not set manually
# VERCEL_URL=
# VERCEL_ENV=

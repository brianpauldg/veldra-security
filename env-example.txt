# ============================================================
# Bloom Metabolics — Environment Variables
# Copy this to .env.local for development
# Set these in Vercel dashboard for production
# NEVER commit .env.local to git
# ============================================================

# ── APP URLs ─────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=https://bloommetabolics.com
# For development:
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── SUPABASE ─────────────────────────────────────────────────
# Get from: supabase.com → your project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — SERVER ONLY, never expose to client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── STRIPE ───────────────────────────────────────────────────
# Get from: dashboard.stripe.com → Developers → API Keys
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-publishable-key

# Stripe Price IDs — create these in Stripe Dashboard → Products
# Consultation: one-time $49 payment
STRIPE_CONSULTATION_PRICE_ID=price_XXXXXXXXXXXXXX
STRIPE_PRICE_CONSULTATION=price_XXXXXXXXXXXXXX

# TRT Program: $149/mo recurring subscription
STRIPE_PRICE_TRT=price_XXXXXXXXXXXXXX

# GLP-1 Program: $249/mo recurring subscription
STRIPE_PRICE_GLP1=price_XXXXXXXXXXXXXX

# ── CALENDLY (for booking after payment) ─────────────────────
NEXT_PUBLIC_CALENDLY_URL=https://calendly.com/your-booking-link

# ── WAITLIST ─────────────────────────────────────────────────
# Business email that receives waitlist lead notifications
BUSINESS_LEAD_EMAIL=brian@bloommetabolics.com

# GoHighLevel inbound webhook URL (Automations → Inbound Webhook trigger)
GHL_WEBHOOK_URL=

# ── CLOUDFLARE TURNSTILE (free bot protection) ──────────────
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ── UPSTASH REDIS (rate limiting — free tier sufficient) ────
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ── ANALYTICS (optional) ─────────────────────────────────────
NEXT_PUBLIC_META_PIXEL_ID=
NEXT_PUBLIC_GA4_ID=
NEXT_PUBLIC_GOOGLE_ADS_ID=
